export interface IContentAccordionProps {
  description: string;
  context: any;
  listName: string;
  accordionTitle: string;
  allowZeroExpanded: boolean;
  allowMultipleExpanded: boolean;
  
  // updateProperty: (value: string) => void;
  // onConfigure: () => void;
}
